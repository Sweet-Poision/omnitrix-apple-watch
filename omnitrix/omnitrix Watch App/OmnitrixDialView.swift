import SwiftUI

struct OmnitrixDialView: View {
    var body: some View {
        ZStack {
            Color.black.edgesIgnoringSafeArea(.all)
            Text("DIAL ACTIVE").foregroundColor(.green)
        }
    }
}
